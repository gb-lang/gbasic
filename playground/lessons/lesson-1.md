# Hello, screen

Goal: print text and clear the canvas with a color.

Use `print()` for your first output and `clear()` to choose the background color.

## Starter Code

```gbasic
print("Hi!")
clear(30, 40, 90)
```

## Solution Code

```gbasic
clear(30, 40, 90)
print("Hello from G-Basic!")
```
